// export default (query, folderData, setFolderData) => {
//     const result = folderData.filter(folder => {
//         folder.name.toLowerCase().includes(query.toLowerCase())
//     })
//     return setFolderData(result)
// }
